import TeamBuilderApp from '../components/TeamBuilderApp';

// Server component by default. Interactive UI lives in the client component.
export default function Page() {
  return <TeamBuilderApp />;
}
